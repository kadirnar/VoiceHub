[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "voicehub"
dynamic = ["version"]
description = "A unified inference and training interface for TTS, ASR, and VAD models"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
license-files = ["LICENSE"]
authors = [{ name = "kadirnardev", email = "kadir.nar@hotmail.com" }]
keywords = [
  "automatic-speech-recognition",
  "speech",
  "text-to-speech",
  "transformers",
  "tts",
  "voice-activity-detection",
  "voice-cloning",
]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Intended Audience :: Science/Research",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Multimedia :: Sound/Audio :: Speech",
  "Typing :: Typed",
]
dependencies = [
  # VoiceHub owns every built-in TTS, ASR, and VAD architecture. PyTorch is
  # the only tensor/autograd substrate in the default inference runtime.
  # Training/reporting tools intentionally live in `voicehub[training]`.
  "torch>=2.8,<2.9",
]

[project.urls]
Homepage = "https://github.com/kadirnar/voicehub"
Repository = "https://github.com/kadirnar/voicehub"
Issues = "https://github.com/kadirnar/voicehub/issues"
Documentation = "https://kadirnar.github.io/voicehub/"

[project.optional-dependencies]
docs = [
  "mkdocs-material==9.7.7",
  "mkdocs-static-i18n==1.3.1",
]
training = [
  "ema-pytorch",
  "datasets",
  "evaluate",
  "jiwer",
  "pyarrow",
  "pyworld",
  "wandb>=0.19",
]
test = [
  "nbformat>=5.10",
  "pre-commit",
  "pytest",
]

[tool.setuptools]
include-package-data = true

[tool.setuptools.dynamic]
version = { attr = "voicehub.__version__" }

[tool.setuptools.packages.find]
where = ["."]
include = ["voicehub*"]
exclude = ["tests*"]
